const UserListCards = () => {
  return (
    <aside className="w-[20%] hidden md:block sticky top-0 h-screen overflow-y-auto bg-gradient-to-b from-gray-900 via-gray-800 to-gray-900 text-white shadow-md">
      <h1 className="p-4 font-semibold text-lg">User List</h1>
      {/* Add user list items here */}
    </aside>
  );
};

export default UserListCards;
